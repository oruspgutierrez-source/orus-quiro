server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Fallback routing para Single Page Applications (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
